# دليل تكامل Zoom - نظام LMS

## ✅ حالة التكامل

تم تكامل Zoom بنجاح مع النظام! جميع المكونات تعمل بشكل صحيح:

- ✅ إعدادات Zoom API (Account ID, Client ID, Client Secret)
- ✅ ZoomService لإدارة الاجتماعات
- ✅ توليد التوقيعات الرقمية (JWT Signatures)
- ✅ Routes للإنشاء والانضمام
- ✅ واجهات عربية كاملة
- ✅ Zoom Web SDK مدمج

---

## 📋 المتطلبات الحالية

### يجب إضافة الصلاحيات التالية لتطبيق Zoom:

اذهب إلى: https://marketplace.zoom.us/ → تطبيقك → Scopes

أضف هذه الصلاحيات:
- ✅ `meeting:write:meeting`
- ✅ `meeting:write:meeting:admin`
- ✅ `meeting:read:meeting`
- ✅ `meeting:read:meeting:admin`

**ملاحظة**: بدون هذه الصلاحيات، لن تتمكن من إنشاء اجتماعات Zoom تلقائياً.

---

## 🚀 كيفية الاستخدام

### 1️⃣ إنشاء جلسة Zoom جديدة

1. افتح: `http://127.0.0.1:8000/admin/sessions/create`
2. املأ معلومات الدرس:
   - عنوان الدرس
   - تاريخ ووقت الجلسة
   - المدة بالدقائق
   - الوصف (اختياري)
3. اختر نوع الدرس: **"Zoom مباشر"**
4. اضغط على زر: **"حفظ الدرس"**
5. ✨ **سيتم إنشاء اجتماع Zoom تلقائياً في الخلفية!**
   - يتم إنشاء الاجتماع تلقائياً بناءً على المعلومات التي أدخلتها
   - يتم ملء جميع بيانات Zoom تلقائياً (Meeting ID, Join URL, Password)
   - لا حاجة لإدخال أي معلومات Zoom يدوياً

### 2️⃣ الانضمام إلى اجتماع Zoom

**من المتصفح مباشرة:**
1. افتح صفحة الجلسة (مثال: `/admin/sessions/2`)
2. أدخل اسمك في حقل "اسمك في الاجتماع"
3. اضغط على: **"الانضمام إلى الاجتماع"**
4. سيفتح Zoom مباشرة في المتصفح!

**عبر تطبيق Zoom:**
1. اضغط على زر: **"الانضمام عبر تطبيق Zoom"**
2. سيفتح تطبيق Zoom تلقائياً

**يدوياً:**
1. افتح تطبيق Zoom
2. اضغط "الانضمام إلى اجتماع"
3. أدخل رقم الاجتماع الموجود في صفحة الجلسة
4. أدخل كلمة المرور إذا طُلبت منك

---

## 🔧 الملفات المحدثة

### Backend (PHP)

1. **`app/Services/ZoomService.php`**
   - إنشاء، تحديث، حذف الاجتماعات
   - توليد التوقيعات الرقمية (JWT)
   - OAuth 2.0 Authentication

2. **`app/Http/Controllers/Api/V1/Admin/ZoomController.php`**
   - `createMeeting()` - إنشاء اجتماع
   - `generateSignature()` - توليد التوقيع للانضمام
   - `getMeeting()` - جلب تفاصيل الاجتماع
   - `deleteMeeting()` - حذف اجتماع

3. **`routes/web.php`**
   ```php
   Route::post('/zoom/create-meeting', [ZoomController::class, 'createMeeting']);
   Route::post('/zoom/generate-signature', [ZoomController::class, 'generateSignature']);
   ```

4. **`config/services.php`**
   ```php
   'zoom' => [
       'account_id' => env('ZOOM_ACCOUNT_ID'),
       'client_id' => env('ZOOM_CLIENT_ID'),
       'client_secret' => env('ZOOM_CLIENT_SECRET'),
       'webhook_secret_token' => env('ZOOM_WEBHOOK_SECRET_TOKEN'),
   ],
   ```

### Frontend (Blade + JavaScript)

1. **`resources/views/admin/sessions/create.blade.php`**
   - نموذج إنشاء الجلسات
   - زر "إنشاء اجتماع Zoom تلقائياً"
   - JavaScript للإنشاء التلقائي

2. **`resources/views/admin/sessions/show.blade.php`**
   - عرض معلومات الاجتماع
   - Zoom Web SDK v3.8.10
   - زر الانضمام المباشر من المتصفح
   - معالجة الأخطاء بالعربية

---

## 🔐 الأمان

### التوقيعات الرقمية (JWT Signatures)

- يتم توليد التوقيع من جانب السيرفر فقط
- مدة صلاحية التوقيع: ساعتين
- يستخدم HS256 encryption
- لا يمكن للمستخدمين رؤية Client Secret

### OAuth 2.0

- استخدام Server-to-Server OAuth
- Access tokens مؤقتة
- تجديد تلقائي للـ tokens

---

## 📊 قاعدة البيانات

جدول `sessions` يحتوي على:
```sql
zoom_meeting_id VARCHAR(255) NULL  -- رقم اجتماع Zoom
zoom_join_url TEXT NULL            -- رابط الانضمام
zoom_password VARCHAR(255) NULL    -- كلمة المرور
```

---

## 🎯 المميزات

### ✅ ما تم تنفيذه:

- ✨ **إنشاء تلقائي كامل لاجتماعات Zoom في الخلفية** (Backend Automation)
- تحديث تلقائي لاجتماعات Zoom عند تعديل الجلسة
- حذف تلقائي لاجتماعات Zoom عند حذف الجلسة
- الانضمام من المتصفح مباشرة (Zoom Web SDK)
- توليد توقيعات آمنة (JWT Signatures)
- واجهات عربية كاملة
- معالجة الأخطاء
- طرق انضمام متعددة (متصفح / تطبيق / يدوي)
- مؤشرات تحميل
- رسائل نجاح وفشل بالعربية

### 🔜 يمكن إضافتها لاحقاً:

- تسجيل الاجتماعات تلقائياً
- إحصائيات الحضور
- Webhooks للإشعارات
- غرفة انتظار مخصصة
- تخصيص خلفية الاجتماع

---

## 🐛 استكشاف الأخطاء

### 1. خطأ: "Invalid access token, does not contain scopes"

**الحل**: أضف الصلاحيات المطلوبة في Zoom Marketplace (انظر قسم المتطلبات أعلاه)

### 2. خطأ: "The meeting number is not found"

**الحل**: تأكد من:
- رقم الاجتماع صحيح
- الاجتماع موجود في حساب Zoom
- لم يتم حذف الاجتماع

### 3. خطأ: "فشل الحصول على التوقيع الرقمي"

**الحل**: تحقق من:
- Client ID و Client Secret في `.env`
- مكتبة `firebase/php-jwt` مثبتة
- السجلات في `storage/logs/laravel.log`

### 4. لا يظهر زر الانضمام

**الحل**: تأكد من:
- الجلسة تحتوي على `zoom_meeting_id`
- الصفحة محدثة (Ctrl+F5)

---

## 📝 أوامر مفيدة

```bash
# فحص حالة التكامل
php artisan tinker
>>> app(\App\Services\ZoomService::class)->generateSignature('test', 0);

# عرض routes الخاصة بـ Zoom
php artisan route:list --name=zoom

# مسح الـ cache
php artisan config:clear
php artisan cache:clear
php artisan route:clear

# عرض السجلات
tail -f storage/logs/laravel.log
```

---

## 🌐 روابط مفيدة

- [Zoom API Documentation](https://developers.zoom.us/docs/api/)
- [Zoom Web SDK](https://developers.zoom.us/docs/meeting-sdk/web/)
- [OAuth 2.0 Guide](https://developers.zoom.us/docs/integrations/oauth/)

---

## ✅ اختبار التكامل

يمكنك اختبار التكامل بتنفيذ:

```bash
php artisan tinker --execute="
echo '=== فحص Zoom ===\n';
echo 'ZoomService: ' . (class_exists('App\Services\ZoomService') ? '✓' : '✗') . '\n';
echo 'JWT Library: ' . (class_exists('Firebase\JWT\JWT') ? '✓' : '✗') . '\n';
echo 'Config: ' . (config('services.zoom.client_id') ? '✓' : '✗') . '\n';
\$sig = app(\App\Services\ZoomService::class)->generateSignature('test', 0);
echo 'Signature: ' . (\$sig ? '✓' : '✗') . '\n';
"
```

---

**تم إنشاء هذا الملف بواسطة**: Claude Code
**التاريخ**: 2025-12-13
**الحالة**: ✅ جاهز للاستخدام (بعد إضافة الصلاحيات)
