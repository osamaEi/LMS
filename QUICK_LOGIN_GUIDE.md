# 🚀 دليل تسجيل الدخول السريع

## معلومات المدير (Admin)

### تسجيل الدخول برقم الهوية:

```
رقم الهوية: 1234567890
كلمة المرور: password123
```

### أو البريد الإلكتروني:

```
البريد: admin@lms.com
كلمة المرور: password123
```

---

## خطوات تسجيل الدخول

1. **شغل السيرفرات:**
   ```bash
   # Laravel
   php artisan serve

   # Vue
   cd lms_front && npm run dev
   ```

2. **افتح المتصفح:**
   ```
   http://localhost:5173/logIn
   ```

3. **أدخل البيانات:**
   - رقم الهوية: `1234567890`
   - كلمة المرور: `password123`

4. **اضغط "تسجيل الدخول"**

5. **سيتم توجيهك إلى:**
   ```
   http://localhost:5173/admin/dashboard
   ```

---

## إنشاء المدير في قاعدة البيانات

إذا لم يكن موجوداً:

```bash
php artisan db:seed --class=SuperAdminSeeder
```

---

✅ **جاهز للاستخدام!**
